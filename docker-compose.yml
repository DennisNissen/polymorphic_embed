version: "3.7"
services:
  db:
    image: postgres:15-alpine
    environment:
        POSTGRES_USER: postgres
        POSTGRES_PASSWORD: postgres
        POSTGRES_DB: polymorphic_embed
    ports:
        - "5432:5432"